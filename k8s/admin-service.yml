apiVersion: v1
kind: Service
metadata:
  name: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
      nodePort: 30083
  type: NodePort