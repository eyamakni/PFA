apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service

spec:
  replicas: 1

  selector:
    matchLabels:
      app: admin-service

  template:
    metadata:
      labels:
        app: admin-service

    spec:
      containers:
        - name: admin-service
          image: maknos/admin-service:latest

          ports:
            - containerPort: 3003

          env:
            - name: DB_HOST
              value: postgres

            - name: DB_PORT
              value: "5432"

            - name: DB_USERNAME
              value: root

            - name: DB_PASSWORD
              value: root

            - name: DB_NAME
              value: velora_postgres